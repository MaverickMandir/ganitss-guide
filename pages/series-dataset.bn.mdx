# সিরিজ এবং ডেটাসেট

import { Card, Cards } from 'nextra-theme-docs'
import units from 'config/units.bn'

## বিভাগ
{units.map(
    (module, index) =>
    index == 4 && (
        <Cards>
            {module.units.map(
                (item, index) =>
                item.unit && (
                    <Card title={item.unit} href={module.slug + "/" + item.key} key={index} />
                )
            )}
        </Cards>
    )
)}